package com.convention

import org.gradle.api.Plugin
import org.gradle.api.Project

class IdeaPluginBaseConventionPlugin : Plugin<Project> {
    override fun apply(target: Project) {
        // todo: migrate to gradle 2.0
    }
}